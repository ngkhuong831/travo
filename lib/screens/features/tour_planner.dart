import 'package:flutter/material.dart';

import '../out_of_service.dart';

class TourPlanner extends StatelessWidget {
  const TourPlanner({super.key});

  @override
  Widget build(BuildContext context) {
    return OutOfServiceScreen();
  }
}
